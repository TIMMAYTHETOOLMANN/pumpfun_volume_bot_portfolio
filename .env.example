cd pumpfun_vbot

# Install dependencies
composer install    # Install PHP dependencies
npm install         # Install Node dependencies
npm run dev         # Compile assets
php artisan key:generate
php artisan migrate
php artisan db:seed
php artisan serve   # Start the local development server

# Open http://localhost:8000 in your browser

# 2. Create a new Laravel project
```bash
composer create-project laravel/laravel my-new-project
cd my-new-project
```

# 3. Install Laravel Breeze
```bash
composer require laravel/breeze --dev
php artisan breeze:install
npm install
npm run dev
php artisan migrate
php artisan serve
```

# 4. Install Laravel Jetstream
```bash
composer require laravel/jetstream
php artisan jetstream:install livewire
npm install
npm run dev
php artisan migrate
php artisan serve
```

# 5. Install Laravel Jetstream with Intertia
```bash
composer require laravel/jetstream
php artisan jetstream:install inertia
npm install
npm run dev
php artisan migrate
php artisan serve
```

# 6. Install Laravel Jetstream with Intertia and Vue3

# 7. Install Laravel Jetstream with Intertia and React
```bash
composer require laravel/jetstream
php artisan jetstream:install inertia --teams
npm install
npm run dev
php artisan migrate
php artisan serve
```
    

# 8. Install Laravel Jetstream with Intertia and React and Livewire
```bash
composer require laravel/jetstream
php artisan jetstream:install inertia --teams
npm install
npm run dev
php artisan migrate
php artisan serve
```




























































































